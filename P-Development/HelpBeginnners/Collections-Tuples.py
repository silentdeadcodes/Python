# Collections is ordered or unordered group of elements(some data types).
# Two types of collections (List and Tuples)
# Tuples = Similar to lists except they're immutable or unchangable
# Tuples uses round brackets

x = (1,4,5,9,3)
print(x[0])

