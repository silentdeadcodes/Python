# Index operator [] = gives access to a sequence's element (str, list, tuples)

name = "slice One Piece"

#if (name[0].islower()):
#    name = name.capitalize()

# we can do slicing strings with index!
first_name = name[0:5].upper()
last_name = name[-5:].lower()
crazy_name = name[-2:].lower()

print(first_name)
print(last_name)
print(crazy_name)











